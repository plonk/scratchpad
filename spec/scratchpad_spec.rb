require 'spec_helper'

describe Scratchpad do
  it 'has a version number' do
    expect(Scratchpad::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
